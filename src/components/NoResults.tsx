import { Container } from '@mui/material';
import React from 'react';

const NoResults = () => {
    return <Container sx={{ height:"100%", textAlign:"center", paddingTop: "2rem"}}>
        No Results
    </Container>
}

export default NoResults;